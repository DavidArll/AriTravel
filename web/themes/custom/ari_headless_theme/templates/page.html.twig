{#
/**
 * @file
 * Theme override to display a single page.
 * For a headless theme, this might output nothing or just essential messages/content.
 */
#}
{# Remove all default page structure. JS app will render everything. #}
{# You could render messages if your JS app doesn't handle them: {{ page.messages }} #}
{# Or a very minimal content region: {{ page.content }} #}
{# For AriTravel, assume the JS app handles all content rendering. #}
